# Audit Tracker — 2026-07 Bug Findings

This file records the closure status of every defect tracked under the
2026-07 architecture audit (GitHub issue #44 / GitLab #54). Each row points
at the PR that fixed it and the regression coverage that keeps it fixed.

| Finding | Issue | Fix PR | Regression test |
|---|---|---|---|
| Frontend calls nonexistent tag-update endpoints | #3 | #52 | `update_entry_tags_handler` / `share_update_entry_tags_handler` route registration |
| Reachable panic: unwrap on nullable hash column | #4 | #53 | `calculate_hash` `Option` handling |
| color-classifier divides by zero on empty pixel buffer | #5 | #54 | `compute_average_empty_returns_none` |
| Unreadable files silently classified as empty | #6 | #56 | `unreadable_file_propagates_error` (`src/job/classify.rs`) |
| EXIF datetime→epoch hand-rolled, no TZ | #7 | #63 | `plugins/exif-classifier/src/lib.rs` `datetime_to_unix` tests |
| Frontend/backend type drift (five mismatches) | #8 | #67 | OpenAPI-generated client (`make openapi-check`) |
| Share expiry semantics differ create vs update | #9 | #72 | `resolve_share_expiry` unit tests |
| Face-match results written to discarded clone | GitLab #11 | #74 | `face_match_results_persist_to_meta_custom` |

All findings were verified against the current `main` before closing the
tracker; `docs/architecture.md` was reviewed for drift and matches the code.
