[package]
name = "byteburrow-plugin-exif"
version = "0.1.0"
edition = "2021"
description = "EXIF photo classifier plugin for ByteBurrow"
authors = ["Martin Miksanik"]

[lib]
crate-type = ["cdylib"]

[dependencies]
byteburrow-plugin-api = { path = "../../byteburrow-plugin-api" }
kamadak-exif = "0.6.1"
serde_json = "1.0"
